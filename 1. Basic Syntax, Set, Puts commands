#######   The set  ||  puts  Command   #######

set design_name ORCA
set PD_Topics {Floorplan, Powerplan, PnR}
set PD_issues "crosstalk setup hold congestion"
set volatge 0.9
set temperature 125c

puts "Starting P&R flow for design: $design_name" 
puts $PD_topics
puts PD_issues
puts $voltage
puts $temperature


if 0 {
Uses of the tcl terms 
; ----> terminate the line
{ } ----> it is used to group the words into single arguments
[ ] ----> to evaluate and execute a command and assign value to variable

Difference b/w [ ] & { }
EX:-set a 10
 set b 20
set [expr $a+$b] ----> o/p shows -30
set {expr $a+$b} –---> o/p shows expr $a+$b
}
