i) print the multiples of 5 and 10 by using nested for loops

        for {set a 0} {$a <= 100} {incr a} {
          for {set b $a} {$b <= $a} {incr b} {
            if {[expr $a % 5 == 0] && [expr $a % 10 == 0]} {
              puts "$b is divisible by 5 and 10"
            }
          }
        }
        
ii) print the multiples of 5 and 10 by using nested for loops

        for { set a 0 } { $a <= 50 } { incr a } {
          for { set b $a } {$b <= $a} {incr b} {
            if {[expr $a%5 == 0 ] && [expr $a%10 == 0 ] } {
              puts "$b is divisible by 5 & 10"
            } elseif {[expr $a%5 == 0 ] } {
              puts "$b is divisible by 5 "
            } elseif { [expr $a%10 == 0 ] } {
              puts "$b is divisible by 10"
            } else {
              puts "$b is not divisible by 5 & 10"
            }
          }
        }

iii) print the multiples of 6 and multiples of 2 and 7 also by using nested loops

        for {set i 0} {$i <= 50} {incr i} {
          for {set j $i} {$j <= $i} {incr j} {
            if {[expr $i % 6 == 0]} {
              puts "$i is divisible by 6 "
            } elseif {[expr $i % 2 == 0]} {
              puts "$i is divisibe of 2"
            } elseif {[expr $i % 7 == 0]} {
              puts "$i is divisibe of 7"
            } 
          }
        }
