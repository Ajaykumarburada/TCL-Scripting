1. Print the count of 1 to 100 by using while with if_elseif_ese conditions and continue, break

    set i 0
    while {$i < 100} {
      incr i
      
      if {$i == 85} {
        puts "the value $i equals to 85 is continue loop"
        continue
      } elseif {$i == 90} {
        puts "The value $i equals to 90 is out of loop"
        break;
      } else {
        puts "count at $i"
      }
    }
    
2. i) display Incrementing count from 1 to 10 using for loop

    for {set i 0} {$i < 10} {incr i} {
      puts "i am at count $i and going up"
    }

                    or
ii) By using while loop
        set i 0
        while {$i < 10} {
            puts $i
            incr i 
        }
3. i) Display decrementing count from 20 to 0 using for loop

    for {set i 20} {$i >= 0} {incr i -1} {
          puts "i am at count $i and going down"
        }

ii) By using while loop

        set i 20
        while {$i >= 0} {
            puts $i
            incr i -1
        }
